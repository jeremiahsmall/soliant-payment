{
    "name": "soliantconsulting/soliant-payment",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "wikimedia/composer-merge-plugin": "^1.3",
        "php": "^5.6 || ^7.0"
    },
    "extra": {
        "merge-plugin": {
            "include": [
                "composer.local.json"
            ],
            "require": [
                "module/PaymentBase/composer.json",
                "module/AuthnetPayment/composer.json"
            ],
            "recurse": true,
            "replace": false,
            "merge-dev": true,
            "merge-extra": false
        }
    }
}